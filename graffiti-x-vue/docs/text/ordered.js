export default function(graffiti) { return {
  template: `
    <h1>Ordered Lists</h1>

    <p>
      In all of the examples so far our the collections we've formed have not have much structure.
      Many were unsorted and some sorted by timestamps.
      However things get trickier when you want a collection of objects that has a particular sort of order.

      TO do this we're going to essentially number.

      But real numbers can't be divided forever, so we're going to use a Logoot CRDT.
      There are some explanations here and here.
    </p>`
}}
